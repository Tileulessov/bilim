<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".lesson.presentation.LessonActivity"
    android:background="@color/orange_bg"
    android:orientation="vertical"
    >
<LinearLayout
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/orange_bg"
    android:orientation="vertical">
    <Toolbar
        android:id="@+id/activity_lesson_toolbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/orange_bg"
        android:navigationIcon="@drawable/ic_arrow_back_left_bold_copy_white"
        android:title="Title"
        android:titleTextColor="@color/MGreyBlue"
        tools:targetApi="lollipop"
        />
    <WebView
        android:id="@+id/activity_lesson_webview"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        />
</LinearLayout>
    <ProgressBar
        android:id="@+id/activity_lesson_progress_bar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:visibility="gone"
        />
</FrameLayout>