<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bilim">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".BilimApplication"
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BilimOnlineCourse"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".sign.SignInActivity"
            android:theme="@style/SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".registration.presentation.RegistrationActivity" />
        <activity android:name=".forgot_password.UserForgotPasswordActivity" />
        <activity android:name=".course_page.presentation.CoursePageActivity" />
        <activity android:name=".lesson.questions.presentation.QuestionsActivity"></activity>
        <activity android:name=".lesson.videoLesson.presentation.VideoLessonActivity" />
        <activity android:name=".user_profile.presentation.UserProfile" />
        <activity android:name=".lesson.presentation.LessonActivity" />
        <activity android:name=".course_content.presentation.CourseContentActivity" />
    </application>

</manifest>